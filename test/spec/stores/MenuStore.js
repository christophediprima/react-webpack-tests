'use strict';

describe('MenuStore', function() {
  var store;

  beforeEach(function() {
    store = require('stores/MenuStore.js');
  });

  it('should be defined', function() {
    expect(store).toBeDefined();
  });
});
